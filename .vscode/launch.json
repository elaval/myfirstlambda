{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to SAM Local",
            "type": "node",
            "request": "attach",
            "address": "localhost",
            "port": 5857,
            "localRoot": "${workspaceRoot}/src",
            "remoteRoot": "/var/task"
        }
    ]
}